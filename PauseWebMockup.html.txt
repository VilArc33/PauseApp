<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Pause — One Verse Bracelet</title>
  <style>
    :root{
      --bg:#f4f2ee;
      --ink:#0b0b0c;
      --muted:rgba(11,11,12,.62);
      --card:rgba(255,255,255,.55);
      --stroke:rgba(11,11,12,.10);
      --shadow: 0 20px 60px rgba(11,11,12,.08);
      --radius: 22px;
    }

    *{ box-sizing:border-box; }
    html,body{ height:100%; }
    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji","Segoe UI Emoji";
      color:var(--ink);
      background:
        radial-gradient(1200px 800px at 50% 20%, #ffffff 0%, rgba(255,255,255,0.35) 40%, rgba(255,255,255,0) 70%),
        linear-gradient(180deg, #f7f6f3 0%, #f0eee9 55%, #ece9e3 100%);
      overflow-x:hidden;
    }

    .wrap{ max-width:1100px; margin:0 auto; padding: 26px 18px 90px; }

    /* Top bar */
    .topbar{
      position:sticky; top:0;
      backdrop-filter: blur(12px);
      background: linear-gradient(180deg, rgba(244,242,238,.78), rgba(244,242,238,.45));
      border-bottom: 1px solid rgba(11,11,12,.06);
      z-index:10;
    }
    .topbar .inner{
      max-width:1100px; margin:0 auto; padding: 14px 18px;
      display:flex; align-items:center; justify-content:space-between;
      gap:14px;
    }

    .mark{
      display:flex; align-items:center; gap:10px;
      font-weight:600; letter-spacing:.2px;
    }
    .squiggle{
      width:28px; height:28px; display:grid; place-items:center;
      border-radius:12px;
      background: rgba(255,255,255,.6);
      border:1px solid rgba(11,11,12,.06);
      box-shadow: 0 8px 24px rgba(0,0,0,.05);
    }
    .squiggle svg{ width:18px; height:18px; opacity:.85; }

    .nav{
      display:flex; align-items:center; gap:18px;
      font-size:14px; color:var(--muted);
    }
    .nav a{ color:inherit; text-decoration:none; }
    .nav a:hover{ color:rgba(11,11,12,.78); }

    .btn{
      appearance:none; border:0; cursor:pointer;
      padding:12px 18px;
      border-radius:999px;
      font-weight:600;
      font-size:14px;
      transition: transform .12s ease, opacity .12s ease;
      white-space:nowrap;
    }
    .btn:active{ transform: translateY(1px); }
    .btn.primary{
      background:#0b0b0c; color:#fff;
      box-shadow: 0 14px 40px rgba(11,11,12,.22);
    }
    .btn.ghost{
      background: rgba(255,255,255,.6);
      border:1px solid rgba(11,11,12,.08);
      color:rgba(11,11,12,.75);
    }

    /* Hero */
    .hero{
      padding-top: 38px;
      display:grid;
      grid-template-columns: 1.2fr .8fr;
      gap: 34px;
      align-items:center;
      min-height: calc(100vh - 90px);
    }

    .headline{
      font-size: clamp(44px, 6vw, 74px);
      line-height: .96;
      letter-spacing: -0.03em;
      margin: 0 0 14px;
    }
    .sub{
      margin:0 0 26px;
      max-width: 52ch;
      font-size: 18px;
      color: var(--muted);
      line-height:1.45;
    }

    .ctaRow{ display:flex; gap:12px; align-items:center; flex-wrap:wrap; }
    .learn{
      display:inline-flex; align-items:center; gap:10px;
      color: rgba(11,11,12,.78);
      text-decoration:none;
      font-weight:600;
      padding: 12px 16px;
      border-radius: 999px;
      background: rgba(255,255,255,.55);
      border: 1px solid rgba(11,11,12,.08);
    }
    .chev{
      width:18px; height:18px;
      border-radius:999px;
      display:grid; place-items:center;
      background: rgba(11,11,12,.06);
    }

    /* Product card */
    .productCard{
      background: var(--card);
      border:1px solid var(--stroke);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      padding: 18px;
    }
    .productFrame{
      border-radius: 18px;
      border:1px solid rgba(11,11,12,.08);
      background: rgba(255,255,255,.55);
      overflow:hidden;
      aspect-ratio: 1 / 1;
      display:grid;
      place-items:center;
    }
    .productFrame img{
      width: 84%;
      height: auto;
      display:block;
      filter: drop-shadow(0 24px 40px rgba(0,0,0,.18));
      transform: translateY(2px);
    }

    .productMeta{
      display:flex; justify-content:space-between; align-items:center;
      margin-top: 14px;
      gap:10px;
    }
    .pill{
      font-size:12px; color: rgba(11,11,12,.65);
      padding: 8px 10px;
      border-radius: 999px;
      background: rgba(255,255,255,.6);
      border:1px solid rgba(11,11,12,.08);
    }

    /* Sections */
    section{ padding-top: 90px; }
    .sectionTitle{
      font-size: 30px;
      letter-spacing:-0.02em;
      margin:0 0 10px;
    }
    .sectionSub{ margin:0 0 22px; color:var(--muted); }

    .grid3{
      display:grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 14px;
    }
    .card{
      background: rgba(255,255,255,.55);
      border:1px solid rgba(11,11,12,.08);
      border-radius: 18px;
      padding: 18px;
      box-shadow: 0 14px 40px rgba(11,11,12,.06);
      min-height: 140px;
    }
    .card h3{ margin:0 0 8px; font-size: 16px; letter-spacing:-.01em; }
    .card p{ margin:0; color:var(--muted); line-height:1.4; font-size:14px; }

    .footer{
      margin-top: 90px;
      padding-top: 22px;
      border-top: 1px solid rgba(11,11,12,.08);
      color: rgba(11,11,12,.55);
      font-size: 13px;
      display:flex; justify-content:space-between; gap:14px; flex-wrap:wrap;
    }
    .footer a{ color: inherit; text-decoration:none; }
    .footer a:hover{ color: rgba(11,11,12,.8); }

    /* Responsive */
    @media (max-width: 860px){
      .hero{ grid-template-columns: 1fr; min-height: unset; padding-top: 24px; }
      .grid3{ grid-template-columns: 1fr; }
      .nav{ display:none; }
    }
  </style>
</head>

<body>
  <div class="topbar">
    <div class="inner">
      <div class="mark">
        <div class="squiggle" aria-hidden="true">
          <svg viewBox="0 0 24 24" fill="none">
            <path d="M6 15c4-8 8 8 12 0" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
          </svg>
        </div>
        <span>Pause</span>
      </div>

      <div class="nav">
        <a href="#about">About</a>
        <a href="#how">How it works</a>
        <a href="#faq">FAQ</a>
      </div>

      <button class="btn primary" onclick="document.querySelector('#preorder').scrollIntoView({behavior:'smooth'})">
        Preorder
      </button>
    </div>
  </div>

  <div class="wrap">
    <main class="hero" id="top">
      <div>
        <h1 class="headline">Pause: One Verse</h1>
        <p class="sub">
          A daily ritual you can wear. Tap your bracelet to open the app — one verse,
          one minute, less noise.
        </p>

        <div class="ctaRow">
          <button class="btn primary" onclick="document.querySelector('#preorder').scrollIntoView({behavior:'smooth'})">
            Preorder bracelet
          </button>

          <a class="learn" href="#about">
            Learn more
            <span class="chev" aria-hidden="true">⌄</span>
          </a>

          <button class="btn ghost" onclick="alert('Replace this with App Store link later')">
            View the app
          </button>
        </div>
      </div>

      <div class="productCard" aria-label="Product preview">
        <div class="productFrame">
          <!-- Replace with your bracelet hero image -->
          <img src="https://via.placeholder.com/900x900.png?text=Bracelet+Image" alt="Pause bracelet" />
        </div>
        <div class="productMeta">
          <div class="pill">NFC tap → app</div>
          <div class="pill">Limited first batch</div>
        </div>
      </div>
    </main>

    <section id="about">
      <h2 class="sectionTitle">Designed to remove pressure.</h2>
      <p class="sectionSub">
        Pause isn’t “finish the Bible in a year.” It’s a simple daily anchor you can actually keep.
      </p>

      <div class="grid3">
        <div class="card">
          <h3>One verse</h3>
          <p>Small enough to do every day. No overwhelming feed.</p>
        </div>
        <div class="card">
          <h3>Quick reflection</h3>
          <p>Write one sentence. Save it. Move on.</p>
        </div>
        <div class="card">
          <h3>Tap to start</h3>
          <p>Your bracelet opens the verse instantly. No searching, no friction.</p>
        </div>
      </div>
    </section>

    <section id="how">
      <h2 class="sectionTitle">How it works</h2>
      <p class="sectionSub">3 steps. That’s the whole product.</p>

      <div class="grid3">
        <div class="card">
          <h3>1) Tap bracelet</h3>
          <p>Opens the app (or the verse page if the app isn’t installed).</p>
        </div>
        <div class="card">
          <h3>2) Read today</h3>
          <p>Mark complete. Optional “Read more” if you want context.</p>
        </div>
        <div class="card">
          <h3>3) Save reflection</h3>
          <p>One line. Mood. Done.</p>
        </div>
      </div>
    </section>

    <section id="preorder">
      <h2 class="sectionTitle">Preorder</h2>
      <p class="sectionSub">Keep it minimal: 1 product, 1 price, 1 checkout.</p>

      <div class="card" style="display:flex; justify-content:space-between; align-items:center; gap:14px; flex-wrap:wrap;">
        <div>
          <div style="font-weight:700; letter-spacing:-.01em;">Pause Bracelet — First Batch</div>
          <div style="color:var(--muted); font-size:14px; margin-top:4px;">Includes NFC setup + app access</div>
        </div>
        <button class="btn primary" onclick="alert('Replace with Stripe Checkout link')">Checkout</button>
      </div>
    </section>

    <section id="faq">
      <h2 class="sectionTitle">FAQ</h2>
      <p class="sectionSub">Answer only what reduces purchase hesitation.</p>

      <div class="grid3">
        <div class="card">
          <h3>Does it work without the app?</h3>
          <p>Yes — it opens a verse page. With the app installed, it opens the app.</p>
        </div>
        <div class="card">
          <h3>What’s on the bracelet?</h3>
          <p>Minimal branding. The point is the ritual, not the logo.</p>
        </div>
        <div class="card">
          <h3>Returns?</h3>
          <p>Keep it simple: 14–30 days, unused condition.</p>
        </div>
      </div>
    </section>

    <div class="footer">
      <div>© <span id="y"></span> Pause: One Verse</div>
      <div style="display:flex; gap:14px;">
        <a href="#" onclick="alert('Add your privacy policy link')">Privacy</a>
        <a href="#" onclick="alert('Add support email / page')">Support</a>
      </div>
    </div>
  </div>

  <script>
    document.getElementById('y').textContent = new Date().getFullYear();
  </script>
</body>
</html>